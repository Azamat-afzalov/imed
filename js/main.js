$(function () {
	"use strict";

	const $document = $(document);

    // document.querySelectorAll('.department-tab').forEach((item, index) => {
    //     console.log(item.getAttribute('data-url'));
    //     item.style.backgroundImage = `url(${item.getAttribute('data-url')})`;
        
    // });

});